# URL base del API (sin barra final). En Docker suele ser la ruta del proxy: /api
VITE_API_URL=/api

# Solo desarrollo local: destino del proxy de Vite (no se usa en el código Vue)
VITE_DEV_BACKEND_TARGET=http://localhost:3000
